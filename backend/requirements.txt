fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
jinja2>=3.1
pydantic>=2.5
sqlalchemy>=2.0
# python-multipart is needed by FastAPI's UploadFile (used by /api/batch).
# pip install fastapi pulls in starlette but not multipart — must be explicit.
python-multipart>=0.0.9
